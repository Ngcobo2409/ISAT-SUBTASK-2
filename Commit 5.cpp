namespace Converter {
    string decimalToBinary(int decimal) { /* existing code */ }
    int binaryToDecimal(const string& binary) { /* existing code */ }
    string decimalToHex(int decimal) { /* existing code */ }
    int hexToDecimal(const string& hex) { /* existing code */ }
    bool isValidBinary(const string& binary) { /* as above */ }
    bool isValidHex(const string& hex) { /* as above */ }
}

// Usage in main:
cout << "Binary: " << Converter::decimalToBinary(decimal) << endl;
